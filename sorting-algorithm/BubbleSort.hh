// https://en.wikipedia.org/wiki/Bubble_sort

#ifndef _BUBBLE_SORT
#define _BUBBLE_SORT

class BubbleSort {
    void BubbleSorting(int[], int);
    public:
        BubbleSort(int[], int);
};

#endif